const express = require("express");

const router = express.Router();

// getUser, getUsers, getUsersByEmail, updateUser, deleteUser, createUser
router.get("/:uid", );
router.get("/", );
router.post("/", );
router.delete("/:uid", );
router.patch("/:uid", );


module.exports = router;